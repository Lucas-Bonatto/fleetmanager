spring:
  datasource:
    url: jdbc:h2:mem:fleetmanager-test;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    baseline-on-migrate: false
    clean-disabled: true
    validate-migration-naming: true
  thymeleaf:
    cache: false
app:
  seed:
    enabled: false
  security:
    admin:
      username: admin-teste
      password: "SenhaTeste@123"
